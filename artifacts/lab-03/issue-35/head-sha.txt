# Evidence head SHA

# Implementation commit validated by this evidence bundle.
# This is the LAST commit that changed implementation code (server/, client/, e2e/).
implementation-sha: 14287867dd31261bb649c7d1eeb96aeb4759ade9

# Commit at which the evidence commands were executed and this bundle was written.
# It is a docs/evidence-only commit: it changes no implementation code.
evidence-captured-at: 14287867dd31261bb649c7d1eeb96aeb4759ade9

# PR #46 head at the time of this capture.
pr-head-at-capture: 14287867dd31261bb649c7d1eeb96aeb4759ade9

# Provenance note (review round 5):
# `1428786` is the last commit that changed implementation code. It contains the three
# round-5 fixes:
#   642351c  fix(lab-03): stop credential-bearing DB URLs from reaching logs/errors
#   031f8b5  fix(lab-03): verify full frozen legacy-to-User mapping before Phase-C resume
#   1428786  fix(client): distinguish session-check failure from unauthenticated on mount
# The evidence below was executed at `1428786` (implementation SHA == evidence SHA), so no
# provenance gap exists for this capture. Any later docs/evidence-only commit does not
# invalidate it (see the bundle README re-run policy).
#
# Security note: the round-5 B-1 fix removed a real DB password that had leaked into the
# previously committed `db-mig-execution.txt` and `server-vitest.txt` via an execSync error
# message. Those logs were regenerated with the fixed code and contain no credential. The
# exposed password must still be treated as compromised and rotated, and repository history
# rewritten (git filter-repo/BFG) — a new commit does not remove it from earlier commits.
